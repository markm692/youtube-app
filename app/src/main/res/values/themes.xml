<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- NoActionBar: Compose draws its own top bar. Without this the system
         ActionBar renders on top of it. -->
    <style name="Theme.YouTubeApp" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
